# WebNoteClient - Web client for DoNote

**WebNoteClient** is a web application built with **MVC**, designed to work seamlessly with the **DoNote API** (`WebNotes`).  
It allows users to manage **notes and tasks**.